import { Component, Input } from '@angular/core';

@Component({
  selector: 'main',
  template: `<xng-breadcrumb></xng-breadcrumb><router-outlet></router-outlet>`,
})
export class MainComponent  {
}
